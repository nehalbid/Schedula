rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // By default, require users to be authenticated to read or write any documents.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}